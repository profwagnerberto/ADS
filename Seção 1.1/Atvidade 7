using System;

class MainClass {
  public static void Main (string[] args) {
    Console.WriteLine( $"Cálculo 1: { 1 - 1.0 / 2.0 }" );
    Console.WriteLine( $"Cálculo 2: { 9 != 9 }" );
    Console.WriteLine( $"Cálculo 3: { true || false }" );
  }
}